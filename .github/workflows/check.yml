name: Check PR

on:
  pull_request:

jobs:
  javapos-build-workflow:
    uses: JavaPOSWorkingGroup/javapos-workflow/.github/workflows/check.yml@main
    with:
      github-event-action: ${{github.event.action}}
    secrets: inherit