name: Build On Push

on:
  push:
    branches: 
      - '**'

jobs:
  javapos-workflow:
    uses: JavaPOSWorkingGroup/javapos-workflow/.github/workflows/build.yml@e4c25a1bd3315a8a8cd6f7bd938ffd5cca4179be
    with:
      github-event-action: ${{github.event.action}}
    secrets: inherit
